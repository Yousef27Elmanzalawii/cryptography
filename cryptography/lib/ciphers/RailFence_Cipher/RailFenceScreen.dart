import 'package:flutter/material.dart';

class Railfencescreen extends StatelessWidget {
  const Railfencescreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}