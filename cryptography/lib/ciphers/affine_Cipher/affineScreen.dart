import 'package:flutter/material.dart';

class Affinescreen extends StatelessWidget {
  const Affinescreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}